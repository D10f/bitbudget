test('On hold until I figure out how to send/receive binary data', () => {})

// const request = require('supertest');
// const app = require('../app');
// const Image = require('../models/Image');
// const { userOne, setupDatabaseForWalletTest } = require('./fixtures/db');
// require('../db/mongoose');
//
// beforeEach(setupDatabaseForWalletTest);
//
// const imageId = '60d7383c1ea3b0118ca87d4e'
//
// test('Should retrieve image for specified id', async () => {
//   const response = await request(app)
//     .get(`/image/${imageId}`)
//     .set('Authorization', `Bearer ${userOne.tokens[0].token}`)
//     .expect(200);
//
//   console.log(response.body);
// });
